# Logging off, for the same reason every other arm turns it off: an arm that
# writes log lines is measuring its logger, not the framework.
pekko {
  loglevel = "OFF"
  stdout-loglevel = "OFF"
  log-dead-letters = 0
  log-dead-letters-during-shutdown = off
}
